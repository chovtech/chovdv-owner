name: 🚀 Deploy to Staging Server via SFTP

on:
  push:
    branches:
      - main  # Or "master" if that’s your default

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v3

      # Skipping 'Install Dependencies' and 'Build' since you are not using Node.js

      - name: 📤 Deploy to Staging via SFTP
        uses: appleboy/scp-action@v0.1.0
        with:
          host: ${{ secrets.SFTP_HOST }}  # SFTP host (e.g., triominds.com.ng)
          username: ${{ secrets.SFTP_USERNAME }}  # SFTP username (e.g., triomind)
          password: ${{ secrets.SFTP_PASSWORD }}  # SFTP password
          port: ${{ secrets.SFTP_PORT }}  # SFTP port (typically 22)
          source: ./  # The local directory to upload from
          target: /public_html/dev.edumess.com/  # The remote directory to deploy to
          debug: true  # Optional: Enables debugging output for troubleshooting
